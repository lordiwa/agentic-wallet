{
  "name": "agentic-wallet",
  "version": "0.1.0",
  "private": true,
  "description": "Agentic Wallet - copiloto financiero personal local-first",
  "workspaces": [
    "server",
    "web",
    "panel"
  ],
  "scripts": {
    "dev": "concurrently -n server,web,panel \"npm run dev --workspace=server\" \"npm run dev --workspace=web\" \"npm run dev --workspace=panel\"",
    "dev:panel": "concurrently -n server,panel \"npm run dev --workspace=server\" \"npm run dev --workspace=panel\"",
    "build": "npm run build --workspace=web && npm run build --workspace=panel && npm run build --workspace=server",
    "build:functions": "npm --prefix functions run build",
    "test:functions": "npm --prefix functions test",
    "test:functions:emulator": "npm --prefix functions run test:emulator",
    "build:mcp": "npm run build:mcp --workspace=server",
    "build:hosting": "npm run build:hosting --workspace=panel",
    "deploy:hosting": "npm run build:hosting && firebase deploy --only hosting",
    "build:hosting:web": "npm run build:hosting --workspace=web",
    "build:docs": "npm --prefix docs-site install --no-audit --no-fund && node docs-site/build.mjs",
    "test": "vitest run",
    "seed": "npm run seed --workspace=server",
    "onboard": "npm run onboard --workspace=server --",
    "review": "npm run review --workspace=server --",
    "gmail-auth": "npm run gmail-auth --workspace=server",
    "reconstruct": "npm run reconstruct --workspace=server --",
    "tenant-snapshot": "npm run tenant-snapshot --workspace=server --"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}
