{
  "compilerOptions": {
    "target": "ES2022",
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "lib": ["ES2023"],
    "outDir": "dist",
    "rootDir": "src",
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "declaration": true,
    "sourceMap": true,
    "tsBuildInfoFile": "dist/.tsbuildinfo"
  },
  "include": ["src/**/*.ts"],
  "//": "`src/test-support/` queda fuera del build a proposito: `paridad.ts` importa el motor de `server/` para poder comparar las dos implementaciones, y eso esta fuera del `rootDir` de este paquete. No es un problema para los tests —vitest resuelve sin `rootDir`— y es lo correcto para el deploy: el andamio de los tests no tiene por que viajar al runtime, igual que `scripts/`.",
  "exclude": [
    "src/**/*.test.ts",
    "src/test-support",
    "dist",
    "src/parser/**/*.ts",
    "src/ingest/mojibake.ts"
  ],
  "references": [{ "path": "./tsconfig.parser.json" }]
}
