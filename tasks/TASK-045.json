{
  "key": "TASK-045",
  "title": "Panel de manejo en Vue 3: consola para operar las habilidades del agente",
  "description": "Mato quiere un panel de manejo del wallet en Vue 3 que no sea sólo de mirar: tiene que permitir OPERAR visualmente las habilidades del agente (sincronizar, revisar la cola, categorizar, conversar, planificar, ahorrar), estar permanentemente actualizado, y ser desplegable — Mato no accede al servidor, todo lo ve publicado.\n\nEl flujo de pantallas completo, los componentes por pantalla, la habilidad del agente que consume cada una, el diagrama de navegación, el ciclo del botón de sync y las decisiones de tiempo real y despliegue están en docs/panel-manejo-flujo.md. Resumen de lo decidido ahí:\n\n- ONCE PANTALLAS: P0 Acceso, P1 Alta y perfil, P2 Resumen, P3 Sincronización, P4 Movimientos, P5 Revisión, P6 Categorías y reglas, P7 Chat, P8 Estrategia, P9 Ahorro y colchón, P10 Configuración (+ P11 Estado del sistema, fase 2). P2 es el hogar; P7 es un cajón lateral que se abre encima de cualquier pantalla arrastrando el contexto de origen.\n- UBICACIÓN: workspace nuevo `panel/` al lado de `web/`. NO se toca web/ (React) — convive hasta que el panel esté probado en uso real; su retiro es otro ticket.\n- TRES HUECOS REALES EN EL MOTOR, verificados leyendo el código:\n  1. NO hay API HTTP de configuración. `onboarding_status`, `suggest_profile`, `set_profile`, `set_rule`, `apply_rules` y `heal_counterparties` viven SÓLO en MCP (stdio) y en los CLIs. Un navegador no habla MCP. P1, P6 y P10 no se pueden construir sin rutas HTTP nuevas en server/src/api/ que llamen a las funciones que ya existen en onboard/ y category/.\n  2. NO hay motor de ahorro (no existe strategy/savings.ts ni tool de sugerencias). Lo que hay: `buffer_status` en /api/overview, POST /api/buffer, y las tablas flexiahorro/saldos/metas sin endpoint. P9 se arma con eso, y las \"sugerencias de ahorro\" son el chat con un prompt específico — se rotulan como respuesta del agente, no como número calculado.\n  3. NO hay autenticación ninguna en la API. Define el alta del usuario y todo el despliegue.\n- ALTA MANUAL DEL USUARIO: un solo usuario, sin registro, sin tabla de usuarios. Se separan dos cosas que la palabra \"alta\" mezcla: (a) ACCESO = una variable `WALLET_ACCESS_TOKEN` que Mato escribe a mano en .env + middleware Bearer sobre /api/* (salvo /api/health); con la variable vacía el server sigue aceptando sólo 127.0.0.1, o sea el comportamiento de hoy sigue siendo el default. (b) PERFIL = P1, que es `npm run onboard` convertido en formulario, conservando su invariante: nunca escribir un valor que el usuario no confirmó.\n- TIEMPO REAL: polling con reloj compartido (portar web/src/lib/refresh.tsx a un composable + store de Pinia, conservando sus tres propiedades: un solo reloj, se para con la pestaña oculta y tickea al volver, y `refreshNow()` para forzar tras cada mutación). SSE sólo para el chat, que es el único stream que el server ya habla. NO se monta canal de eventos del ledger: el único que escribe es el sync que dispara este mismo panel, no hay un tercero produciendo cambios. Se agrega un GET /api/events el día que el sync corra por cron, no antes.\n- DESPLIEGUE, la tensión honesta: \"panel desplegable\" y \"server en 127.0.0.1\" chocan, y el choque no es del panel — es que la API no tiene auth. Recomendación en tres fases SIN saltearse ninguna: fase 0 panel publicado en modo demo (cero riesgo, la UI se opera entera con datos inventados); fase 1 datos reales por Tailscale (`tailscale serve` hacia localhost, sin abrir puerto público, sin tocar WALLET_BIND_HOST); fase 2 `WALLET_ACCESS_TOKEN` ANTES de cualquier otra exposición. Al revés hay una ventana con el ledger en internet sin llave. Túnel público, snapshot horneado en el bundle y 0.0.0.0 sin auth quedan descartados por escrito.\n\nORDEN DE CONSTRUCCIÓN: (1) server — rutas faltantes + token; (2) esqueleto panel/ + capa api/ portada + modo demo; (3) P2+P3+P5, que ya reemplazan funcionalmente a la web actual; (4) P4+P7; (5) P8+P9; (6) P1+P6+P10; (7) P0.\n\nEste ticket es el PLAN. La implementación se puede partir en tickets hijos por fase; los criterios de abajo son de alto nivel y valen como definición de terminado del panel completo.",
  "acceptance_criteria": [
    "Existe el workspace `panel/` (Vue 3 + <script setup> + TypeScript + Vite) declarado en `workspaces` de package.json, y `panel/src/**/*.test.ts` está agregado al `include` de vitest.config.ts — sin eso los tests del panel no corren.",
    "`web/` (React) NO se modifica en ninguno de los commits del panel: sigue construyendo y sus tests siguen verdes. El retiro de web/ es otro ticket.",
    "Las rutas HTTP faltantes existen en server/src/api/ y sólo validan y serializan — cero lógica financiera nueva: onboarding (status/suggestions/profile GET y POST) sobre server/src/onboard/ + db/strategy-config.ts, reglas (listar/crear/borrar/aplicar/heal/sin-categorizar) sobre server/src/category/ + ingest/heal-counterparty.ts, deudas (GET /api/debts y el deshacer del pagado) sobre la tabla debts, el calendario de pagos sobre strategy/calendar.ts, y el histórico de ahorro sobre las tablas flexiahorro/saldos/metas. Cada ruta con su test. La auditoría completa, hueco por hueco (H1..H26), está en docs/panel-viabilidad.md.",
    "El panel NO dibuja ninguna acción sin respaldo backend. Antes de construir cada pantalla se verifica su fila en docs/panel-viabilidad.md: lo que quedó como hueco no resuelto no se dibuja, o se dibuja explícitamente deshabilitado diciendo POR QUÉ no está disponible todavía. Un botón que no hace nada es peor que un botón ausente.",
    "P5/ReviewCard: la comparación 'lo que leyó el parser' vs 'lo que leyó Claude' y el chip de motivo sólo se muestran si existe el respaldo persistido (columnas review_reason y claude_amount escritas por el pipeline y devueltas por GET /api/review). Hoy review_reason se calcula en ingest/pipeline.ts y NO se persiste, y la lectura de Claude no se guarda en ningún lado. Mientras no existan, la tarjeta muestra raw_subject y las acciones, y dice que el motivo no está disponible — no inventa una explicación. El cuerpo del correo NO se persiste (es dato personal): el enlace al correo se hace con gmail_msg_id.",
    "P8: la lista de deudas se alimenta de un GET /api/debts real — hoy se puede marcar una deuda pagada por id y no hay forma de listar los ids. La columna 'Vence' sólo se dibuja si la tabla debts tiene fecha de vencimiento (hoy no la tiene). 'Simular' se limita al único parámetro que el motor sabe proyectar (abono); las perillas de gasto mensual y aporte al colchón son aritmética nueva y van en strategy/ con sus tests, en otro ticket — jamás en un computed del cliente.",
    "P9: 'Fijar objetivo de colchón' escribe strategy_config.colchonObjetivo por la ruta de perfil, NO por POST /api/buffer — ese endpoint escribe savings.reserved, y el objetivo que muestra colchonStatus sale de strategy_config (strategy/balance.ts). Si se ofrece 'Registrar aporte', la suma la hace el server, nunca el cliente. Metas e histórico de aportes sólo se dibujan si existen sus endpoints; si no, la pantalla dice que las tablas están sin exponer.",
    "P3: 'Detener' dice la verdad sobre lo que detiene. Sin endpoint de cancelación, corta el auto-encadenado y se rotula 'se detiene al terminar este lote' — no promete abortar el lote en vuelo. El 'Registro de lotes' persiste en el server (GET /api/sync/runs) o se rotula como registro de esta sesión que se pierde al recargar; no se dibuja una tabla histórica que en realidad vive en memoria.",
    "P4/FilterBar: la barra sólo ofrece filtros que la API acepta. El filtro por categoría, la selección múltiple de tipo, el autocompletar de contraparte y el 'Mostrando 8 de N' requieren backend nuevo (filtro category, type repetible, GET /api/counterparties, y un `total` real en GET /api/transactions, que hoy devuelve el tamaño de la página). 'Interna' no es una dirección: va en el toggle include_internal.",
    "P0/P10: GET /api/health informa si el server exige token (auth_required), para que P0 pida la frase cuando sirve y diga que es decorativa cuando no. El checklist de P1 y P10 renderiza los pasos que devuelve el motor (env/claude/gmail/sync/huso/profile), no una lista escrita a mano en el panel. NO se construye ningún endpoint que borre o rehaga el ledger, ni uno que marque a mano una fila como needs_review — ver docs/panel-viabilidad.md §4.",
    "`WALLET_ACCESS_TOKEN` existe en config.ts y .env.example, y un middleware exige `Authorization: Bearer` en todo /api/* salvo /api/health. Con la variable VACÍA el comportamiento es el de hoy (sólo 127.0.0.1) — el token habilita exponerse, no es cosmético. Cubierto por tests que verifican 401 sin token y 200 con token.",
    "Las once pantallas del doc existen y están ruteadas; cada una consume la habilidad del agente que el doc le asigna y ninguna calcula un total, una proyección o una categoría en el cliente.",
    "El botón de sync cubre los ocho estados del doc (al día / atrasado / nunca / a medias / corriendo / 409 sync_already_running / 503 gmail_not_configured / 500). `last_sync_ts: null` se muestra como 'nunca sincronizaste', NO como una fecha; `progress.complete === false` ofrece 'Seguir' con el conteo real y no finge que terminó.",
    "Cuando un lote de sync deja filas en needs_review, el panel muestra un aviso PERSISTENTE (no un toast) con acción directa a P5, y no desaparece hasta que la cola queda vacía.",
    "El estado del sync se rehidrata de GET /api/sync/status al montar: recargar la página en medio de un backlog muestra el backlog, no un estado limpio falso.",
    "El reloj de refresco es un composable + store de Pinia con las tres propiedades portadas de web/src/lib/refresh.tsx: uno solo para todo el panel, se detiene con la pestaña oculta y dispara un tick al volver, y expone `refreshNow()` que se llama tras cada mutación (resolver revisión, guardar regla, fijar colchón, terminar un lote de sync). Con test que verifique la pausa y el tick al volver.",
    "Hay indicador de frescura permanente ('actualizado hace X') y cartel de desconexión cuando el backend no responde: el panel nunca muestra números viejos con cara de actuales.",
    "El editor de reglas muestra EN VIVO cuántas filas del ledger matchea el patrón que se está escribiendo, antes de guardarlo — un patrón más largo que la contraparte real nunca matchea y eso tiene que ser visible al escribirlo, no después.",
    "`amount: null` se muestra como 'sin leer' en toda la UI, nunca como cero; las filas en needs_review se muestran marcadas y excluidas de los totales, igual que el motor las excluye.",
    "El panel construye y se publica en modo demostración (`VITE_API_BASE_URL=demo`, la mecánica de web/src/demo/demoFetch.ts portada) y el artefacto publicado NO contiene ningún dato real ni ninguna URL privada: el backend se elige con `?api=` desde el navegador de quien mira.",
    "P9 rotula las sugerencias de ahorro como respuesta del agente (chat), no como un cálculo del sistema — no se inventa un motor de ahorro que no existe.",
    "Ningún valor personal se loguea ni se hornea: nada de nombres, montos o correos en telemetría, fixtures o bundle. Los fixtures del panel usan nombres ficticios.",
    "El prototipo de interacción (Claude Design) cubre los CINCO FLUJOS CLAVE de docs/flujo-app-prototipo.md §2 de punta a punta, clickeables: (1) sync — botón → corriendo → a medias → Seguir → completo, con la bifurcación del aviso persistente hacia P5; (2) revisión — una fila de la cola resuelta y el overview cambiando por esa resolución; (3) chat — pregunta, respuesta en streaming, y la propuesta del agente que NAVEGA a la pantalla que confirma la acción en vez de ejecutarla; (4) estrategia/ahorro — proyección, ajustar el objetivo del colchón y ver el impacto en el anillo y en P2; (5) reglas — el contador de coincidencias en vivo mostrando N > 0, luego 0 con la advertencia del patrón demasiado largo, y la previsualización antes de aplicar. Cada flujo enlaza las tarjetas del design system en el orden de §4.2.",
    "El prototipo dibuja los estados obligatorios de cada componente (docs/flujo-app-prototipo.md §3.1): los 8 del SyncButton, las variantes de ReviewCard incluyendo la de SIN MOTIVO disponible, los 5 de OverviewCard, los estados vacíos de §3.4, y los duplicados por estado listados en §4.3. Todo punto sin respaldo backend (§3.5) se dibuja con datos ficticios marcados y su número de hueco (H1..H26) visible — el prototipo nunca muestra como resuelto algo que la auditoría dejó abierto. El cuerpo del correo no se dibuja en ninguna variante de ReviewCard: es dato personal.",
    "`npm run build` y `npm test` en verde, con los tests del panel incluidos."
  ],
  "status": "todo",
  "priority": "high",
  "labels": [
    "panel",
    "vue3",
    "frontend",
    "plan-listo",
    "requiere-api-nueva",
    "despliegue",
    "viabilidad-auditada"
  ],
  "assignee": null,
  "depends_on": [],
  "linked_commits": [],
  "linked_prs": [],
  "comments": [
    {
      "author": "orquestador",
      "body": "Planificación completada en docs/panel-manejo-flujo.md. Tres hallazgos que cambian el alcance respecto de lo asumido al pedirlo: (1) la mitad de las habilidades del agente (onboarding, reglas, heal) sólo existen en MCP por stdio y necesitan rutas HTTP nuevas antes de que el panel pueda usarlas; (2) no hay motor de ahorro — P9 se apoya en el colchón y en el chat, y se rotula como tal; (3) la API no tiene autenticación, así que 'panel desplegable' y 'server en 127.0.0.1' sólo conviven en modo demo hasta que exista WALLET_ACCESS_TOKEN. Recomendación registrada: fase 0 demo publicado, fase 1 Tailscale, fase 2 token antes de cualquier otra exposición, en ese orden.",
      "created_at": "2026-09-02T00:00:00.000Z"
    },
    {
      "author": "orquestador",
      "body": "Auditoría de viabilidad completada en docs/panel-viabilidad.md: las 11 páginas y los 6 componentes del diseño mapeados contra el código real de server/src/. Veredicto: 4 piezas VIABLES hoy (P2 Resumen, P7 Chat, OverviewCard, ChatPanel), 9 PARCIALES (P3, P4, P5, P8, P9, P10, SyncButton, TransactionsTable, FilterBar) y 4 NO VIABLES (P0, P1, P6, ReviewCard). 26 huecos con endpoint propuesto (H1..H26).\n\nCuatro hallazgos que el plan original no tenía y que cambian el alcance del bloque de server:\n\n1. LA REVIEWCARD NO TIENE RESPALDO. Es la pieza que el diseño declara 'donde la invariante del motor se vuelve visible', y sus tres elementos distintivos no se persisten: review_reason se calcula en ingest/pipeline.ts y se pierde (no hay columna en transactions ni la escribe insertTransaction), el monto que leyó Claude no se guarda en ningún lado, y del correo sólo queda raw_subject. La comparación de dos columnas que define la tarjeta no tiene de dónde salir.\n\n2. NO SE PUEDEN LISTAR LAS DEUDAS. Existe POST /api/debts/:id/paid y ningún GET: hay un botón que escribe por id y ninguna forma de conocer los ids. Además la tabla debts no tiene fecha de vencimiento, que es una columna del diseño de P8.\n\n3. 'FIJAR OBJETIVO DE COLCHÓN' ESCRIBE DONDE EL MOTOR NO LEE. POST /api/buffer escribe savings.reserved, pero el objetivo que muestra colchonStatus sale de strategy_config.colchonObjetivo (strategy/balance.ts) — una clave que hoy sólo se escribe por MCP o CLI. El botón más visible de P9 apunta a la columna equivocada.\n\n4. LA TABLA DE MOVIMIENTOS NO PUEDE PAGINAR NI FILTRAR COMO SE DIBUJÓ. GET /api/transactions devuelve count = tamaño de la página (no el total de coincidencias), no acepta filtro por categoría, no acepta type múltiple y no hay endpoint de contrapartes para el autocompletar.\n\nConsecuencia práctica: el bloque 3 del orden de construcción (P2+P3+P5, 'el núcleo operativo') deja de ser gratis. P2 y P7 son las dos únicas pantallas que se construyen sin tocar el server. Se recomienda NO construir dos cosas del diseño: el 'rehacer el ledger desde cero' de la zona de riesgo (borrar el ledger por HTTP en una API sin auth es el botón que no hay que exponer) y el 'mandar a revisión' manual desde P4 (needs_review es una afirmación del pipeline sobre dos lecturas del correo, no una etiqueta a gusto).",
      "created_at": "2026-09-02T00:00:00.000Z"
    },
    {
      "author": "orquestador",
      "body": "Flujo de la aplicación documentado en docs/flujo-app-prototipo.md — el guion de interacción para armar el prototipo clickeable en Claude Design. Cierra el hueco entre el plan (qué pantallas hay) y la auditoría (con qué se alimentan): faltaba EN QUÉ ORDEN SE TOCAN LAS COSAS.\n\nQué agrega:\n\n1. DIAGRAMA DE NAVEGACIÓN en tres capas: la entrada condicional (P0 sólo si auth_required, P1 sólo si el checklist está incompleto, y P1 nunca bloquea — 'Saltar por ahora' siempre lleva a P2), el abanico desde el hogar con qué elemento concreto de P2 lleva a cada pantalla, y la barra permanente. Más una tabla de en qué pantallas aparece cada componente y en qué forma (el SyncButton vive en tres lugares con el mismo estado; el ChatPanel es el mismo componente en dos envases).\n\n2. CINCO FLUJOS PASO A PASO con la bifurcación explícita en cada uno. El del sync incluye la rama de la revisión pendiente y el rótulo honesto de 'Detener' ('se detiene al terminar este lote'). El de revisión insiste en mostrar el overview ANTES y DESPUÉS: resolver tiene que mover el número, o la pantalla no se entiende. El de chat fija la regla de que el agente PROPONE y navega a la pantalla que confirma — nunca ejecuta desde el cajón, que es la invariante del onboarding aplicada al chat. El de reglas pone el contador en vivo como el corazón del flujo, con sus tres estados.\n\n3. ESTADOS OBLIGATORIOS por componente, micro-interacciones (30 s normal / 3 s durante sync / pausa con pestaña oculta / refreshNow tras cada mutación / rebote de 300 ms en el contador), diez estados vacíos, y una tabla de LOS 20 PUNTOS DONDE EL PROTOTIPO SE QUEDA SIN BACKEND, cada uno con su hueco H1..H26 y cómo representarlo con datos ficticios marcados.\n\n4. MAPEO a las 18 tarjetas del design system, con el orden de conexión flujo por flujo y los duplicados por estado que hacen falta para que el prototipo se recorra entero (P3 ×4, P6 ×4, C5 ×4, C1 ×8, etc.).\n\nDos decisiones de interacción nuevas que no estaban en los docs anteriores: (a) el aviso de revisión pendiente es una FRANJA PERSISTENTE NO CERRABLE bajo la barra, no un toast — es una tarea pendiente y se comporta como tal hasta que la cola quede en cero; (b) el patrón de 'propuesta del agente' es un bloque reutilizable cuyo botón dice 'Revisar y crear' y navega, nunca escribe.",
      "created_at": "2026-09-02T00:00:00.000Z"
    }
  ],
  "created_at": "2026-09-02T00:00:00.000Z",
  "updated_at": "2026-09-02T00:00:00.000Z",
  "jira_key": null,
  "verification_tier": "tests-after"
}
